# Secrets — NEVER commit this
.env

# Python
__pycache__/
*.pyc
.venv/

# Local data / outputs
*.db
*.db-wal
*.db-shm
reports/

backups/

# Game source clones + node_modules used only to build web/games/* (the built
# bundles ARE committed; their multi-hundred-MB sources are not).
.gamebuild/
node_modules/
